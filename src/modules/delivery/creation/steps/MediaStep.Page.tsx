import { ScreenLayout } from '@components/layout';
import { MediaPicker } from '@widgets/MediaPicker';

export const MediaStepPage = () => {
	return (
		<ScreenLayout>
			<MediaPicker />
		</ScreenLayout>
	);
};
