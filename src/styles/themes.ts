import { tokens } from '@/styles/tokens';

export const dark = {
	bgMain: tokens.color.graphite900,
	bgContent: tokens.color.graphite700,
	bgCard: tokens.color.graphite800,

	textPrimary: tokens.color.graphite50,
	textSecondary: tokens.color.graphite300,

	btnBgSecondary: tokens.color.white12,
	btnBgFocusSecondary: tokens.color.white8,
	btnTextSecondary: tokens.color.graphite50,
	btnBgPrimary: tokens.color.orange500,
	btnTextPrimary: tokens.color.white,

	outline: tokens.color.orange800,

	tabText: tokens.color.graphite800,
	tabBg: tokens.color.white16,
	tabBgActive: tokens.color.white,

	inputBg: tokens.color.graphite600,

	separator1: tokens.color.graphite400,
	separator2: tokens.color.ink300,

	backgroundColor: tokens.color.graphite900,
};
export const light = {
	bgMain: tokens.color.graphite900,
	bgContent: tokens.color.graphite700,

	textPrimary: tokens.color.graphite50,
	textSecondary: tokens.color.graphite300,

	btnBgSecondary: tokens.color.white12,
	btnBgFocusSecondary: tokens.color.white8,
	btnTextSecondary: tokens.color.graphite50,
	btnBgPrimary: tokens.color.orange500,
	btnTextPrimary: tokens.color.white,

	outline: tokens.color.orange800,

	tabText: tokens.color.graphite800,
	tabBg: tokens.color.white16,
	tabBgActive: tokens.color.white,

	inputBg: tokens.color.graphite600,

	separator1: tokens.color.graphite400,
	separator2: tokens.color.ink300,

	backgroundColor: tokens.color.graphite900,
};
